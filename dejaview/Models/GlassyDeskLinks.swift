import Foundation

enum GlassyDeskLinks {
    static let macSetup = URL(string: "https://bunn.dev/glassydesk/mac/")!
    static let remoteSetup = URL(string: "https://bunn.dev/glassydesk/mac/#tailscale")!
    static let tailscaleDownload = URL(string: "https://tailscale.com/download")!
}
